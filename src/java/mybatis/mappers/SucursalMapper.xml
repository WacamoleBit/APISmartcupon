<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sucursal">

    
    <insert id="registrarSucursal" parameterType="modelo.pojo.DatosRegistroSucursal" statementType="CALLABLE">
           {CALL registrarSucursal(
            #{sucursal.nombre, mode=IN, jdbcType=VARCHAR},
            #{direccion.calle, mode=IN, jdbcType=VARCHAR},
            #{direccion.numero, mode=IN, jdbcType=INTEGER},
            #{direccion.codigoPostal, mode=IN, jdbcType=VARCHAR},
            #{direccion.colonia, mode=IN, jdbcType=VARCHAR},
            #{direccion.ciudad, mode=IN, jdbcType=INTEGER},
            #{direccion.tipoDireccion, mode=IN, jdbcType=INTEGER},
            #{sucursal.telefono, mode=IN, jdbcType=VARCHAR},
            #{sucursal.latitud, mode=IN, jdbcType=DOUBLE},
            #{sucursal.longitud, mode=IN, jdbcType=DOUBLE},
            #{persona.nombre, mode=IN, jdbcType=VARCHAR},
            #{persona.apellidoPaterno, mode=IN, jdbcType=VARCHAR},
            #{persona.apellidoMaterno, mode=IN, jdbcType=VARCHAR},
            #{persona.tipoPersona, mode=IN, jdbcType=INTEGER},
            #{filasAfectadas, mode=INOUT, jdbcType=INTEGER}
    )}
    </insert>
    
    <insert id="registrarPersonaSucursal" parameterType="modelo.pojo.Persona">
        INSERT INTO persona (nombre, apellidoPaterno, apellidoMaterno, tipoPersona) 
        VALUES (#{nombre}, #{apellidoPaterno}, #{apellidoMaterno}, #{tipoPersona})
    </insert>
    
    <update id="modificarSucursal" parameterType="modelo.pojo.Sucursal">
        UPDATE sucursal SET nombre=#{nombre}, direccion=#{direccion}, telefono=#{telefono}, latitud=#{latitud}, longitud=#{longitud}, 
        encargado=#{encargado} 
        WHERE idSucursal=#{idSucursal}
    </update>
 
    <update id="modificarEncargadoSucursal" parameterType="modelo.pojo.Persona">
        UPDATE persona SET nombre=#{nombre}, apellidoPaterno=#{apellidoPaterno},apellidoMaterno=#{apellidoMaterno} 
        WHERE idPersona = #{idPersona} 
    </update>
    
    <delete id="eliminarSucursal" parameterType="modelo.pojo.Sucursal">
        DELETE FROM sucursal WHERE idSucursal = #{idSucursal}
    </delete>

</mapper>
