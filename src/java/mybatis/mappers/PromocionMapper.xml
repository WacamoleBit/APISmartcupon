<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="promocion">
    
    <insert id="registrarPomocion" parameterType="modelo.pojo.Pomocion">
        INSERT INTO promocion (nombre, descripcion, imagen, fechaInicio, fechaTermino, restricciones, tipoPromocion, porcentajeDescuento, categoria, cuponesDisponibles, codigoPromocion, estatus, empresa) 
        VALUES (#{nombre}, #{descripcion}, #{imagen}, #{fechaInicio}, #{fechaTermino}, #{restricciones}, #{tipoPromocion}, #{porcentajeDescuento}, #{categoria}, #{cuponesDisponibles}, #{codigoPromocion}, #{estatus}, #{empresa}) 
    </insert>


</mapper>
