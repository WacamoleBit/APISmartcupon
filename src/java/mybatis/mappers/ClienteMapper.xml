<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cliente">
    
    <select id="verificarClienteExistente" resultType="modelo.pojo.Cliente" parameterType="map">
        SELECT * 
        FROM cliente 
        WHERE email = #{email}
    </select>
    
    <select id="obtenerClientePorId" resultType="modelo.pojo.Cliente" parameterType="int" >
        SELECT * 
        FROM cliente 
        WHERE idCliente = #{idCliente}
    </select>
    
    <insert id="registrarCliente" parameterType="modelo.pojo.Cliente" >
        INSERT INTO cliente (nombre, apellidoPaterno, apellidoMaterno, telefono, email, direccion, fechaNacimiento, password) 
        VALUES (#{nombre}, #{apellidoPaterno}, #{apellidoMaterno}, #{telefono}, #{email}, #{direccion}, STR_TO_DATE(#{fechaNacimiento}, '%Y-%m-%d'), #{password})
    </insert>
    
    
    <update id="modificarCliente" parameterType="modelo.pojo.Cliente">
        UPDATE cliente SET nombre=#{nombre}, apellidoPaterno=#{apellidoPaterno}, apellidoMaterno=#{apellidoMaterno}, 
        telefono=#{telefono}, direccion=#{direccion}, fechaNacimiento=#{fechaNacimiento}, password=#{password} 
        WHERE idCliente=#{idCliente}
    </update>
    
</mapper>